{
  "name": "dwc-button",
  "type": "module",
  "exports": {
    "./button": {
      "import": "./dist/button.js",
      "require": "./dist/button.cjs"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist/*",
    "index.d.ts",
    "logger.d.ts",
    "math.d.ts"
  ],
  "version": "1.0.4",
  "scripts": {
    "prepare": "yarn run build",
    "clean:dist": "rimraf dist/",
    "build:types": "tsc --declarationDir dist/ --emitDeclarationOnly --declaration",
    "build:lib": "vite build",
    "build": "yarn run clean:dist && yarn run build:lib && yarn run build:types"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "autoprefixer": "^10.4.12",
    "lit": "^2.4.0",
    "postcss": "^8.4.18",
    "sass": "^1.55.0",
    "tailwindcss": "^3.2.0",
    "ts-lit-plugin": "^1.2.1",
    "typescript": "^4.8.4",
    "vite": "^3.1.8"
  }
}